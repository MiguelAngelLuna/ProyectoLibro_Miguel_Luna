<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical">

        <!-- Imagen grande del libro -->
        <ImageView
            android:id="@+id/ivImagenInfo"
            android:layout_width="match_parent"
            android:layout_height="300dp"
            android:scaleType="centerCrop"
            android:adjustViewBounds="true"
            android:layout_marginBottom="16dp"
            android:clipToOutline="true"
            android:background="@drawable/rounded_shadow" />

        <!-- Título -->
        <TextView
            android:id="@+id/tvTituloInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Título del Libro"
            android:textColor="#FFFFFF"
            android:textSize="24sp"
            android:textStyle="bold"
            android:layout_marginBottom="4dp" />

        <!-- Autor -->
        <TextView
            android:id="@+id/tvAutorInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="por Autor"
            android:textColor="#BBBBBB"
            android:textSize="16sp"
            android:layout_marginBottom="8dp" />

        <!-- Categoría -->
        <TextView
            android:id="@+id/tvCategoriaInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Categoría"
            android:textColor="#FF6F61"
            android:textStyle="bold"
            android:textSize="15sp"
            android:layout_marginBottom="8dp" />

        <!-- Precio -->
        <TextView
            android:id="@+id/tvPrecioInfo"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="S/ 00.00"
            android:textColor="#FFD700"
            android:textStyle="bold"
            android:textSize="20sp"
            android:layout_marginBottom="12dp" />

        <!-- Descripción -->
        <TextView
            android:id="@+id/tvDescripcionInfo"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="Descripción detallada del libro..."
            android:textColor="#E0E0E0"
            android:textSize="15sp"
            android:lineSpacingExtra="4dp"
            android:background="@drawable/bg_description"
            android:padding="12dp"
            android:layout_marginTop="8dp"
            android:layout_marginBottom="16dp"
            android:gravity="start" />

    </LinearLayout>
</ScrollView>
